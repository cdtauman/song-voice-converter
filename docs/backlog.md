# Backlog

רעיונות ושיפורים שעלו תוך כדי עבודה ו**לא** נכנסים לשלב הנוכחי.

**הכלל:** אם משהו לא נדרש כדי לעמוד ב-Definition of Done של השלב הפעיל — הוא נרשם כאן
וממשיכים. מה שנרשם כאן לא נעלם, אבל גם לא עוצר את הפיתוח.

> **הוסר מהרשימה (16.8.2026):** "תמיכה ב-Intel Arc" **אינה** פריט backlog.
> Intel Arc / Core Ultra הוא **יעד חומרה מרכזי** של SongVoice, לצד NVIDIA.
> המימוש נמצא ב-`svc_engine/compute/` ומאומת ב-Compatibility Spike.
>
> **הושלם ב־Phase 10:** telemetry של RAM/VRAM במערכת benchmark, תשתית
> `env-bench`, ומטריצת השוואה כללית. ההרצות על מנועים ושמע אמיתי נשארות חסם
> קבלה חיצוני בדוח Phase 10, לא חוב מימוש.
>
> **הושלם ב־Phase 11:** ffmpeg LGPL נעול, SHA-256 לכל קובץ שניתן להפצה,
> מתקיני online/offline ו־updater עם checksum/rollback. חתימת קוד וכתיבת המראה
> הפרטי נשארות פעולות חיצוניות בדוח Phase 11.

| # | פריט | מאיזה שלב עלה | למה נדחה | מתי לשקול |
|---|------|----------------|-----------|------------|
| 1 | מסלול Intel נוסף דרך OpenVINO / DirectML לרכיבים ש-XPU לא מצליח להריץ | Phase 1 | המסלול הרשמי של PyTorch XPU נבדק קודם | אם רכיב כלשהו נשאר על CPU אחרי Phase 2 |
| 3 | חתימה דיגיטלית של מודלים מעבר ל־SHA-256 | Phase 1 | Phase 11 אוכפת checksum וגודל; חתימת מודל דורשת תשתית מפתחות נפרדת ואינה נדרשת ל־MVP | אחרי v1, אם מופעל ערוץ הפצה עצמאי |
| 4 | תמיכה ב-`SwiftF0` כמנוע F0 | Phase 1 | ייבחן בבנצ'מרק ולא יוחלט מראש | Phase 3 |
| 7 | חתימת קוד (Code Signing) למניעת אזהרת SmartScreen | Phase 0 | כלי ו־CI הוכנו ב־Phase 11; חסרה תעודת Authenticode חיצונית | לפני פרסום installer |
| 8 | **מודל DeReverb מתירני** — המקור הטוב הוא GPL-3.0, ו-DeEcho הוא CC-BY-NC | Phase 2 | Phase 11 אינה אורזת אותו וממשיכה בבטחה בלי השכבה; חלופות MIT שנמצאו הן לדיבור ולא נמדדו על שירה | אחרי v1 או עם אישור בעלים/מודל ווקאל מתירני חדש |
| 9 | לאמת באוזן שמודל הקאראוקה לא מחזיר ליד↔מלווים הפוך | Phase 2 | דורש שיר עם מקהלה (שיר בדיקה #2) | כשיתקבלו חומרי הבדיקה |
| 10 | להשלים mirror פרטי למודלים הניתנים להפצה | Phase 2 | כל קובץ חוקי להפצה נעול ב־SHA וכלי audit/upload הוכן; חסרים repo/token חיצוניים | לפני פרסום installer online |
| 12 | לשדרג את הוכחת ההפרדה על XPU מ-`ops` ל-`end_to_end` במטריצה | Phase 2 | ההוכחה בוצעה ותועדה, אבל `svc verify-backends` עדיין מריץ רק בדיקת אופרטורים | כשיתווסף עומס מודל-אמיתי ל-`compute/verify.py` |
| 13 | האצת מאמן Applio על Intel XPU במקום fallback ל-CPU | Phase 9 | Applio תומך CUDA/CPU ואינו מצהיר מסלול XPU; שינוי לולאת האימון אינו נדרש ל-DoD ומסוכן בלי benchmark אמיתי | Phase 10 — למדוד זמן/זיכרון ולבחון adapter מבודד |
